{% extends 'layout.html.twig' %}

{% block title %}RentRelease{% endblock %}

{% block body %}
    <h1>RentRelease</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ rent_release.id }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ rent_release.Amount }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ rent_release.status }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ rent_release.date ? rent_release.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('rent_release_index') }}">back to list</a>
{% endblock %}
