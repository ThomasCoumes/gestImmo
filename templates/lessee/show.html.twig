{% extends 'base.html.twig' %}

{% block title %}Lessee{% endblock %}

{% block body %}
    <h1>Lessee</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ lessee.id }}</td>
            </tr>
            <tr>
                <th>Civility</th>
                <td>{{ lessee.civility }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ lessee.name }}</td>
            </tr>
            <tr>
                <th>Lastname</th>
                <td>{{ lessee.lastname }}</td>
            </tr>
            <tr>
                <th>Birthday</th>
                <td>{{ lessee.birthday ? lessee.birthday|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>PlaceOfBirth</th>
                <td>{{ lessee.placeOfBirth }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ lessee.email }}</td>
            </tr>
            <tr>
                <th>PhoneNumber</th>
                <td>{{ lessee.phoneNumber }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('lessee_index') }}">back to list</a>

    <a href="{{ path('lessee_edit', {'id': lessee.id}) }}">edit</a>

    {{ include('lessee/_delete_form.html.twig') }}
{% endblock %}
