<form class="deleteForm col-md-9 col-lg-9 col-9 col-sm-9 col-xl-9" method="post" action="{{ path('lessee_delete', {'id': lessee.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ lessee.id) }}">
    <button class="btn text-primary"><i class="far fa-trash-alt"></i> Supprimer</button>
</form>
